A type which can be used as a key\_type canbe used to subscript a map.The subscript operator return mapped\_type.  
    map<string,int> world_counts;
    string str("hello");
    int cnt=world_counts[str];

